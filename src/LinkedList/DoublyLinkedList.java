package LinkedList;

public class DoublyLinkedList {
    public static class Nodes{
        int val;
        Nodes next;
        Nodes prev;
        Nodes(int val){
            this.val = val;
        }
    }

    public static void main(String[] args) {
        Nodes a = new Nodes(4);
        Nodes b = new Nodes(10);
        Nodes c = new Nodes(2);
        Nodes d = new Nodes(99);
        Nodes e = new Nodes(13);

         
    }
}
